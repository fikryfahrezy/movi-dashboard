pre-commit:
  parallel: true
  jobs:
    - root: "back-end/"
      stage_fixed: true
      glob:
        - "*.md"
        - "*.json"
        - "*.yaml"
      run: npx prettier --write {staged_files}

    - root: "back-end/"
      glob:
        - "*.html"
        - "*.css"
        - "*.ts"
        - "*.js"
      group:
        piped: true
        jobs:
          - run: npx eslint {staged_files}
          - run: npx prettier --write {staged_files}
            stage_fixed: true
